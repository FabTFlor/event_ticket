spring.application.name=eventtickets
# Configuración de PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/event_tickets
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Desactivar la autenticación automática de Spring Security
#pring.security.user.name=admin
#pring.security.user.password=admin

